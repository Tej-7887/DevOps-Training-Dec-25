   16  sudo apt update
   17  sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
   18  docker --version
   19  serivce docker status
   20  service docker status
   21  vi index.html
   22  ls
   23  cat index.html
   24  touch Dockerfile
   25  ls
   26  apt update -y
   27  ls
   28  vi Dockerfile
   29  ls
   30  docker build -t web-app .
   31  vi Dockerfile
   32  docker build -t web-app .
   33  docker images
   34  docker run -d --name login-app -p 80:80 web-app:latest
   35  docker ps
   36  ls
   37  rm index.html
   38  ls
   39  history


# ==================================

   31  vi docker-compose.yml
   32  ls
   33  cat docker-compose.yml
   34  ls
   35  docker compose --version
   36  docker-compose --version
   37  curl -SL https://github.com/docker/compose/releases/download/v5.0.0/docker-compose-linux-x86_64 -o /usr/local/bin/docker-compose
   38  chmod +x /usr/local/bin/docker-compose
   39  docker-compose --version
   40  ls
   41  docker-compose up -d
   42  docker images
   43  docker ps
   44  vi docker-compose.yml
   45  docker-compose up -d
   46  docker ps
   47  docker exec -it mongodb -- mongosh
   48  docker exec -it mongodb --mongosh
   49  docker exec -it mongodb mongosh
   50  ls
   51  history


# mognodb commands  must be run inside the mongodb container

docker exec -it mongodb mongosh

show dbs

use transactionhistory

show collections

db.transactions.find().pretty()